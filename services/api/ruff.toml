line-length = 100
target-version = "py312"
exclude = ["alembic/versions"]

[lint]
select = ["E", "F", "W", "I", "UP", "B"]
# B008: FastAPI relies on Depends() in default arguments.
ignore = ["E501", "B008"]
